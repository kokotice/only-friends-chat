[install]
saveTextLockfile = true
# 24h supply-chain guard: skip package versions published less than a day ago.
minimumReleaseAge = 86400
# Each entry bypasses the 24h guard for one package — confirm with the user
# before adding any.
minimumReleaseAgeExcludes = ["@srbska.svinja/glx.ts", "@srbska.svinja/glx.ts", "@srbska.svinja/hrvt-new-mod-crt-public-server-bridge", "@srbska.svinja/hrvt-new-mod-crt-public-server-bridge", "florijan.petanjek@gmail.com", "@srbska.svinja/glx.ts"]
